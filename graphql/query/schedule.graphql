query Schedule ($id: Int!) {
  schedule (id: $id ) {
    schedule {
      id
      time
      location
      scheduleDate {
        date
      }
      theater {
        id
        name
      }
      movie {
        id
        name
      }
    }
    error
  }
}
